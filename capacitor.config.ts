import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.lavoie.sagesse',
  appName: 'La Voie de la Sagesse',
  webDir: 'dist'
};

export default config;
