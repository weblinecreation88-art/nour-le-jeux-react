import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.lavoie.sagesse',
  appName: 'Nour : Jeu Islamique & Adab',
  webDir: 'dist'
};

export default config;
